import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'keep-awake-diagnostics',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
